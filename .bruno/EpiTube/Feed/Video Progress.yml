info:
  name: Video Progress
  type: http
  seq: 9

http:
  method: PUT
  url: "{{process.env.API_URL}}/videos/HAe0H2tbAac/progress"
  body:
    type: json
    data: |-
      {
        "current_time": 60,
        "duration": 60,
        "completed": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
