info:
  name: Video Liked
  type: http
  seq: 11

http:
  method: POST
  url: "{{process.env.API_URL}}/videos/LkAop9hkFUr/like"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
