info:
  name: Random
  type: http
  seq: 5

http:
  method: GET
  url: "{{process.env.API_URL}}/random"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
