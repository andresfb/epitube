info:
  name: Videos Tagged
  type: http
  seq: 7

http:
  method: GET
  url: "{{process.env.API_URL}}/tags/stranger"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
