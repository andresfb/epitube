info:
  name: Duration
  type: http
  seq: 3

http:
  method: GET
  url: "{{process.env.API_URL}}/duration/medium"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
