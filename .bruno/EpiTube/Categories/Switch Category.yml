info:
  name: Switch Category
  type: http
  seq: 6

http:
  method: GET
  url: "{{process.env.API_URL}}/switch/straight"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
