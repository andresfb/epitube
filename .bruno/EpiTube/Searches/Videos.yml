info:
  name: Videos
  type: http
  seq: 1

http:
  method: GET
  url: "{{process.env.API_URL}}/search?term=lara"
  params:
    - name: term
      value: lara
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
